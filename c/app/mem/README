00 初始化时申请头, 数据部分[数据块]内存,
   申请时检查未使用则直接使用                           [空间利用率不够高。块大小是固定的]
   释放逻辑释放数据部分
   销毁释放内存

01 初始化时申请头, 数据部分[数据块]内存,
   申请时检查未使用部分, 使用且在数据结构中标记数据块是否分化    [block_size/num]  [提高空间利用率, 块的再使用]
   释放时除了标记外mapping, 需要检查是否整块释放, 也需要标记
   销毁释放内存
note:与00比较 数据结构需要一个映射部分, 分块记录.

02 初始化时只初始化头[头内大小固定], 数据部分在申请时申请.
   申请时直接挂到头中，并标记是否使用
   释放时，释放数据。
   销毁释放内存
note:与00比较 数据结构中增加一个申请大小，                        [io的申请次数增加]

03 初始化时只初始化头, 数据部分在申请时申请.
   申请时头需要对应的修改，如果不够需要重申请。[此处有两种方式倍增，线形加]
   释放时，只释放数据部分
   销毁释放内存
note:与00比较 结构头现在放在了申请中修改。

04 03初始 申请，释放时采用01的方式 
   03，01结合
